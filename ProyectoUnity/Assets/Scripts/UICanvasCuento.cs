using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UICanvasCuento : UIScreenBaseController
{
    // Start is called before the first frame update
    public void ShowPanel( int i)
    {
        ShowUIComponents(i);
    }
}
